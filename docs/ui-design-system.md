# UI 与长时间阅读体验规范

版本：0.1
设计关键词：安静、清晰、可信、专注、轻量。

## 1. 设计目标

界面应把资料、上下文范围和来源放在中心，而不是突出 AI 的“人格感”。用户长时间阅读时，需要较低视觉噪音、舒适行宽、稳定布局和可预测交互；生成和索引过程则用轻量反馈消除等待焦虑。

### 1.1 原则

1. **内容优先**：主内容区域拥有最高视觉层级，操作控件在需要时出现。
2. **范围始终明确**：问答与出题页面持续显示当前使用哪些资料。
3. **状态可理解**：上传、解析、生成、失败都说明当前阶段和下一步。
4. **渐进展开**：引用、详细设置、元数据默认折叠，避免一次展示全部信息。
5. **阅读稳定**：流式输出不造成页面横跳；侧栏、段落和滚动位置稳定。
6. **动效有意义**：只解释状态变化和空间关系，不做持续漂浮、闪烁或装饰动画。

## 2. 信息架构

```text
主导航
├─ 资料库
│  ├─ 全部资料
│  ├─ 集合
│  └─ 上传/处理队列
├─ 学习空间
│  ├─ 阅读与问答
│  └─ 历史对话
├─ Quiz
│  ├─ 创建
│  ├─ 待完成
│  └─ 历史与错题
└─ 设置
   ├─ 账户与数据
   └─ 阅读偏好
```

桌面主框架采用左侧导航 + 中央正文 + 可选右侧上下文面板。阅读/问答时，右侧面板在资料问答与引用定位之间切换；普通列表页不强制保留右侧面板。

窄屏将左侧导航收进抽屉，问答与引用面板作为临时右侧抽屉显示，避免三栏压缩正文。

## 3. 页面布局

### 3.1 尺寸

- 全局内容最大宽度：`1440px`。
- 普通表单/设置内容：`720–840px`。
- 长文正文理想行宽：中文约 `32–40` 个全角字符，CSS 可用 `max-width: 42rem` 起步并通过可用性测试调整。
- 左侧导航：展开 `240px`，收起 `72px`。
- 右侧问答/引用面板：当前使用约 `390px`，拖动调整宽度属于后续优化。
- 8px 基础间距系统：4、8、12、16、24、32、48、64。

长文页面不使用整屏超宽文本。用户可以选择“紧凑 / 舒适 / 宽松”阅读密度，但每种模式保持合理行宽。

### 3.2 阅读与问答布局

```text
┌──────────┬──────────────────────────────┬──────────────────┐
│ 导航     │ 文档正文                     │ 问答 / 引用定位   │
│          │                              │                  │
│ 资料列表 │ 标题、来源单元、正文          │ 提问、回答、引用   │
│          │ 保持独立滚动位置              │ 独立滚动         │
└──────────┴──────────────────────────────┴──────────────────┘
```

引用点击后先在右侧预览原文，不强制把用户从当前阅读位置带走。选择“在原文中打开”后跳到正文并高亮匹配摘录；收起问答可返回打开前的阅读位置。

## 4. 字体与排版

### 4.1 字体栈

优先使用系统字体，减少加载和跨语言渲染问题：

```css
--font-sans: Inter, ui-sans-serif, -apple-system, BlinkMacSystemFont,
  "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
--font-mono: "SFMono-Regular", Consolas, "Liberation Mono", monospace;
```

正文不要使用过细字重。中文长文默认：

- 正文 `17px / 1.8`，字重 400。
- 辅助文字 `14px / 1.55`。
- H1 `32px / 1.25`，H2 `24px / 1.35`，H3 `20px / 1.45`。
- 段落间距约 `0.8–1em`；列表项间保留呼吸感。

支持用户调整字号（至少 15–21px）、行高和主题，并将偏好持久化。浏览器缩放到 200% 时内容仍可用。

## 5. 色彩系统

采用低饱和中性色作为大面积背景，使用克制的蓝绿色表达主操作与引用。不要大面积使用纯白/纯黑，降低长时间阅读的眩光与对比疲劳。

### 5.1 浅色主题 Token

```css
:root {
  --bg-canvas: #f6f7f5;
  --bg-surface: #ffffff;
  --bg-subtle: #eef1ed;
  --text-primary: #202522;
  --text-secondary: #5d675f;
  --text-muted: #78827a;
  --border-default: #dce2dd;
  --accent: #256d5a;
  --accent-hover: #1e5b4b;
  --accent-soft: #e3f1ec;
  --focus-ring: #2f806a;
  --success: #26734d;
  --warning: #9a6412;
  --danger: #b24242;
  --info: #356ca5;
  --selection: #cfe7de;
}
```

### 5.2 深色主题 Token

```css
[data-theme="dark"] {
  --bg-canvas: #151816;
  --bg-surface: #1d211e;
  --bg-subtle: #252b27;
  --text-primary: #e8ede9;
  --text-secondary: #b5beb7;
  --text-muted: #8f9a92;
  --border-default: #343c36;
  --accent: #70b9a3;
  --accent-hover: #8bc9b6;
  --accent-soft: #203c34;
  --focus-ring: #87cdb8;
  --success: #72bd8e;
  --warning: #d9a653;
  --danger: #e18282;
  --info: #7da9d5;
  --selection: #315848;
}
```

最终颜色需要通过对比度自动检查。状态不能只靠颜色，应同时使用图标、文字或形状。

## 6. 组件规范

### 6.1 按钮

- Primary 每个可视区域原则上只有一个，表示明确的下一步。
- Secondary 用于并列但非主要操作；Tertiary 用于工具型操作。
- 危险操作使用文字明确的按钮，例如“删除资料”，不只放垃圾桶图标。
- 异步提交后按钮进入 loading 且防止重复提交；长任务跳转到可追踪状态，而不是无限转圈。

### 6.2 资料卡片/列表

默认优先列表视图，便于大量资料扫描。每行展示名称、类型、集合、处理状态、页数/大小和更新时间；次要操作进入菜单。

处理状态使用“图标 + 文案 + 进度阶段”。失败状态直接提供查看原因和重试，不使用只有红点的模糊提示。

### 6.3 问答消息

- 回答以正常文章排版呈现，不使用大面积聊天气泡。
- 用户问题可用轻微底色区分，宽度仍保持阅读舒适。
- 引用采用紧凑编号标记，键盘可聚焦，悬停/聚焦显示来源摘要。
- 流式输出按段落稳定追加；不要让光标持续闪烁或自动滚动抢夺用户位置。
- 生成中提供“停止”；完成后提供复制、重新生成和反馈。

### 6.4 Quiz

- 每屏聚焦一个主要任务，可逐题作答，也可在侧栏查看进度。
- 选项点击区域至少 44px 高，选中状态同时有边框、背景、图标。
- 提交前显示未答数量；提交后答案、解析、来源分层展示。
- 正确/错误不能只用绿/红，必须加图标和文字。

### 6.5 空、加载、错误状态

- 空状态说明价值和下一步，如“上传第一份资料，开始带引用问答”。
- 骨架屏仅用于结构已知且短暂的加载；长任务显示阶段、可离开提示和后台通知。
- 错误文案结构：发生了什么、是否影响数据、用户现在能做什么、必要时提供错误编号。

## 7. 动效规范

动效用于反馈、层级和空间连续性：

| 场景 | 时长 | 建议 |
| --- | --- | --- |
| hover / press | 100–140ms | 颜色、阴影小幅变化 |
| tooltip / popover | 140–180ms | 淡入 + 2–4px 位移 |
| 侧栏 / sheet | 200–260ms | ease-out 进入，ease-in 退出 |
| 列表新增/状态变化 | 180–240ms | 淡入，高亮一次后稳定 |
| 页面切换 | 160–220ms | 轻微淡入，不做大幅滑动 |

禁止：持续呼吸动画、阅读区视差、长时间闪烁、无意义 loading 动画、生成文本逐字弹跳。

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    scroll-behavior: auto !important;
    transition-duration: 0.01ms !important;
  }
}
```

## 8. 无障碍与键盘体验

- 目标达到 WCAG AA；设计和开发阶段持续自动检查并辅以键盘/读屏人工测试。
- 所有交互元素可通过键盘访问，焦点顺序符合视觉顺序，焦点环清晰可见。
- Modal/Drawer 打开时管理焦点，关闭后归还触发元素；Esc 可关闭非破坏性浮层。
- 图标按钮有可访问名称；装饰图标对读屏隐藏。
- Toast 不承担唯一错误信息；表单错误与字段关联并提供汇总。
- 流式回答使用谨慎的 live region，避免读屏器逐 token 播报。
- 快捷键不得覆盖浏览器/辅助技术常用键，并提供可发现的快捷键帮助。

## 9. 响应式策略

- `< 768px`：单栏；导航、问答和引用使用临时抽屉；底部操作避免遮挡系统区域。
- `768–1099px`：可折叠导航；正文 + 临时问答/引用面板。
- `≥ 1100px`：三栏阅读布局；问答或引用面板可常驻。
- 表格在小屏转换为信息卡或保留关键列，不能简单缩到不可读。

## 10. 当前实现基线

- 阅读页的单资料问答使用独立右侧栏，与引用定位共用右栏并分别滚动；长回答不会挤动正文。打开问答时记录来源单元和阅读位置，收起时恢复；窄屏侧栏作为可用 Esc 关闭的临时面板。每个结论的引用按钮先在来源面板预览原文，再由用户选择“在原文中打开”；此时高亮对应来源和可匹配的摘录。
- 回答在引用校验完成后整体呈现；支持停止、复制回答与引用、失败后重新提问和最近 20 条记录。首版不展示未验证的流式文本；无证据时说明不足，不显示伪引用。

- 资料库采用上传引导区与最近资料卡片，展示真实上传进度、解析阶段、页数、大小和可解释失败。
- 阅读器桌面端采用 `240px / 正文 / 390px` 三栏，正文默认 `17px / 1.8 / 42rem`；窄屏将导航与引用转换为临时面板。
- 字号范围为 `15–21px`，正文行宽为窄、舒适、宽三档；字号、行宽和深浅主题持久化在浏览器本地。
- `[`、`]` 逐页导航，`R` 切换引用面板，`?` 打开快捷键说明，`Esc` 关闭临时面板。所有图标按钮提供可访问名称。
- 阅读器按当前页加载正文而不是一次拉取整份资料；翻页时保留总页数与当前位置，适合长资料并控制浏览器内存。
- 上传期间禁用重复选择并允许取消；上传成功后进入可恢复的处理页，处理状态由服务端数据轮询更新，完成后自动显示正文与页码，不用刷新页面。
- 删除使用明确的不可恢复确认对话框；移动端导航和引用面板打开后转移焦点、限制焦点范围，并可用 `Esc` 返回原操作位置。

不要只按设备名设计；用内容在断点处是否拥挤作为判断依据。

## 11. 文案规范

- 使用用户语言：“资料处理中”，不显示“embedding pipeline running”。
- 按钮使用动词：“生成 Quiz”“重新处理”“查看引用”。
- AI 不确定时直说：“在所选资料中没有找到足够依据”。
- 删除确认说明影响范围和可恢复性，避免笼统的“你确定吗”。
- 同一概念只用一个名称：统一使用“资料”“集合”“Quiz”“引用”。

## 11. 设计验收清单

- 主任务在 5 秒内可被首次使用者识别。
- 所有页面包含 loading、empty、error、disabled 和 success 状态。
- 资料范围与处理状态在关键流程始终可见。
- 正文行宽、字号、行高适合连续阅读 30 分钟以上的可用性测试。
- 浅色、深色、高缩放、键盘和减少动态效果模式均通过检查。
- 动效不造成布局位移，不延迟用户操作，不影响读取。
- 引用可追溯、可键盘访问，并保持用户原来的阅读位置。

## 12. 标准参考

- [W3C WCAG 2.2](https://www.w3.org/TR/WCAG22/)：以 AA 作为产品验收基线，覆盖颜色、对比度、重排、焦点与输入等要求。
- [W3C 可辨识内容说明](https://www.w3.org/WAI/WCAG22/Understanding/distinguishable)：用于检查长文正文、控件、链接和状态提示的可感知性。
