# 项目工程审查

日期：2026-09-26。范围：当前仓库与统一学习 Agent 的待提交改动。

## 结论

项目具备专业 MVP 的工程基础：模块化单体、独立 API/Worker、明确模型端口、版本化迁移和 Prompt、认证与 workspace 隔离、锁定依赖、分层测试、CI 及部署/备份手册。该结论针对工程实现与本地验证，不代表真实模型质量或生产环境运维已验收。

## 本次审查修复

- Quiz 创建在读取幂等记录前锁定 workspace，串行化同空间创建与重试，避免相同请求并发落库触发唯一约束错误。隔离 PostgreSQL 回归检查同时提交两个相同请求，返回同一个 Quiz，记录数量为 1。
- 容器 CI 增加学习流程联调：统一对话、出题、作答、评分、取消、私有审计、跨空间隔离、共享配额和来源删除。模型使用合成响应，不产生云端调用。
- 容器 CI 增加独立数据库的上一版本迁移验证，保留已有 Quiz 和消息，并检查 Alembic 元数据一致性；沿用任务结束后的容器和卷清理。

## 验证证据

- Ruff、格式检查、严格 mypy 检查、97 项后端测试通过；20 条版本化 Agent 评测样本结构验证通过。
- 前端构建与 14 项组件测试通过。统一对话改动已有 4 条浏览器流程通过，覆盖键盘、答案隐藏、来源定位与 390px 窄屏；另 1 条上传阅读流程因未提供 `E2E_PDF_PATH` 跳过。
- 空库升级和从 `0012` 的已有数据升级至 `0013` 已验证。本次并发修复再次通过上一版本升级和完整 PostgreSQL/Redis 学习联调；临时容器及独立卷已清理。
- `uv audit --locked` 与前端生产依赖审计未发现已知漏洞；结果是检查当时的漏洞数据库状态，不替代权限/数据隔离测试。
- Compose 配置、本地文档链接、提交差异检查通过；提交排除凭据、原始用户资料、缓存与构建产物。

## 上线前仍需完成

1. 用授权真实资料运行付费模型评测，人工核对引用是否支持结论、题目/答案质量与自然语言任务识别，记录费用和时延。
2. 校准供应商真实费用，配置全局日预算和通知目标；当前 Agent 已有单任务调用/Token/相对费用单位限额和数据库出题配额。
3. 在目标云服务器落实域名/HTTPS、秘密文件、异机加密备份与监控，并完成实际恢复演练。按 [上线与数据恢复手册](./production-operations.md) 验收。
4. 部署先升级数据库至 `0013`，再更新应用和前端；应用回退保留兼容新增列。详见 [Agent 开发计划](./agent-development-plan.md)。

GitHub 分支保护和 CI 结果须按实际远程状态确认；本次审查不修改仓库设置、不合并 `main`。
