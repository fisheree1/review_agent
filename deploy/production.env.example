# Non-secret production configuration. Copy outside the repository as production.env.
PUBLIC_DOMAIN=study.example.com
PROD_SECRETS_DIR=/etc/review-agent/secrets
APP_IMAGE_TAG=local
POSTGRES_DB=review_agent
STORAGE_BUCKET=review-agent-documents
